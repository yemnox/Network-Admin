#working with python 3.7 
python3.7 -m venv snmp37
source snmp37/bin/activate

python --version

#Test if tkinter is installed
# If tkinter is installed, this command will open a small window.
python -c "import tkinter; tkinter._test()"


#SNMP Packages
pip install pysnmp
